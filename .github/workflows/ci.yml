name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  # SOLID スコアは絶対値(min-score)で門番せず、PR で base ブランチからの
  # 回帰(相対評価)だけを見る。スコアが下がった分は PR コメントで可視化する。
  solid:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: SOLID score (PR diff)
        uses: harakeishi/go-solid-score@e56a9290595857b5a0f6aa39dfbf40aeb4aae5d1 # v0.3.0
        with:
          version: v0.3.0
          mode: diff
          # 可視化優先の運用: 回帰してもジョブを fail させず、必ず PR コメントで
          # スコア差分(REGRESSED 行含む)を出す。回帰で fail すると diff が
          # exit してコメント投稿に到達しないため fail-on-regression は false。
          # max-drop は分類(REGRESSED/UNCHANGED)の境界で、コメント表示にのみ影響する。
          max-drop: "20"
          fail-on-regression: "false"
          comment: "true"

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build
        run: go build -v ./...

      - name: Test
        run: go test -v -race -coverprofile=coverage.out ./...

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.out

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest

  vet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Vet
        run: go vet ./...
